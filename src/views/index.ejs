<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PokeDex</title>
</head>
<body>
    <h1>PokeDex Unicesumar</h1>
    <% results.forEach(pokemon => { %>
    <div>
        <a href="/detalhar/<%=pokemon.name%>">Nome: <%= pokemon.name %></a>
        <p>URL: <%= pokemon.url %></p>
    </div>
    <% }) %>
</body>
</html>